# ==============================================
# BotManager V3.0 - DeepSeek V3.2 ONLY
# Minimal Dependencies - Zero Bloat
# ==============================================

# Core Web Framework
flask==3.0.0
flask-cors==4.0.0

# HTTP Requests
requests==2.31.0

# Environment Variables
python-dotenv==1.0.0

# YAML Support (for file_manager.py)
pyyaml==6.0.1

# ==============================================
# That's It! No OpenAI, No Anthropic, No LangChain
# Just DeepSeek V3.2 via direct REST API
# ==============================================

# Optional - For Production Deployment
# gunicorn==21.2.0

# Optional - For ZIP Download (already in Python stdlib)
# Nothing needed - uses built-in zipfile

# ==============================================
# Total: 5 packages only!
# vs Old: 40+ packages (bloated)
# ==============================================
